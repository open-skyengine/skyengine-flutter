name: Android

on:
  push:
    branches:
      - '**'
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  verify:
    name: Analyze and test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze

      - name: Run tests
        run: flutter test

  publish-continuous:
    name: Publish continuous APK
    runs-on: ubuntu-latest
    needs: verify
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Configure Android signing
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          test -n "$ANDROID_KEYSTORE_BASE64"
          test -n "$ANDROID_KEYSTORE_PASSWORD"
          test -n "$ANDROID_KEY_ALIAS"
          test -n "$ANDROID_KEY_PASSWORD"
          echo "$ANDROID_KEYSTORE_BASE64" | base64 --decode > android/release.jks
          {
            echo "storeFile=../release.jks"
            echo "storePassword=$ANDROID_KEYSTORE_PASSWORD"
            echo "keyAlias=$ANDROID_KEY_ALIAS"
            echo "keyPassword=$ANDROID_KEY_PASSWORD"
          } > android/key.properties

      - name: Build and prepare release APKs
        run: |
          mkdir -p dist
          flutter build apk --release --target-platform android-arm,android-arm64 --build-name 1.0.16-continuous --build-number ${{ github.run_number }}
          cp build/app/outputs/flutter-apk/app-release.apk dist/skyengine-continuous.apk
          flutter build apk --release --target-platform android-arm64 --build-name 1.0.16-continuous --build-number ${{ github.run_number }}
          cp build/app/outputs/flutter-apk/app-release.apk dist/skyengine-continuous-arm64-v8a.apk
          flutter build apk --release --target-platform android-arm --build-name 1.0.16-continuous --build-number ${{ github.run_number }}
          cp build/app/outputs/flutter-apk/app-release.apk dist/skyengine-continuous-armeabi-v7a.apk

      - name: Move continuous tag
        run: |
          git tag --force continuous "$GITHUB_SHA"
          git push --force origin refs/tags/continuous

      - name: Update continuous release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: continuous
          name: Continuous Build
          prerelease: true
          make_latest: false
          files: |
            dist/skyengine-continuous.apk
            dist/skyengine-continuous-arm64-v8a.apk
            dist/skyengine-continuous-armeabi-v7a.apk

  publish-release:
    name: Publish release APK
    runs-on: ubuntu-latest
    needs: verify
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Verify tag is on main
        run: git merge-base --is-ancestor "$GITHUB_SHA" origin/main

      - name: Configure Android signing
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          test -n "$ANDROID_KEYSTORE_BASE64"
          test -n "$ANDROID_KEYSTORE_PASSWORD"
          test -n "$ANDROID_KEY_ALIAS"
          test -n "$ANDROID_KEY_PASSWORD"
          echo "$ANDROID_KEYSTORE_BASE64" | base64 --decode > android/release.jks
          {
            echo "storeFile=../release.jks"
            echo "storePassword=$ANDROID_KEYSTORE_PASSWORD"
            echo "keyAlias=$ANDROID_KEY_ALIAS"
            echo "keyPassword=$ANDROID_KEY_PASSWORD"
          } > android/key.properties

      - name: Derive version
        id: version
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          echo "name=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Build and prepare release APKs
        run: |
          mkdir -p dist
          flutter build apk --release --target-platform android-arm,android-arm64 --build-name ${{ steps.version.outputs.name }} --build-number ${{ github.run_number }}
          cp build/app/outputs/flutter-apk/app-release.apk "dist/skyengine-${GITHUB_REF_NAME}.apk"
          flutter build apk --release --target-platform android-arm64 --build-name ${{ steps.version.outputs.name }} --build-number ${{ github.run_number }}
          cp build/app/outputs/flutter-apk/app-release.apk "dist/skyengine-${GITHUB_REF_NAME}-arm64-v8a.apk"
          flutter build apk --release --target-platform android-arm --build-name ${{ steps.version.outputs.name }} --build-number ${{ github.run_number }}
          cp build/app/outputs/flutter-apk/app-release.apk "dist/skyengine-${GITHUB_REF_NAME}-armeabi-v7a.apk"

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          prerelease: false
          make_latest: true
          files: |
            dist/skyengine-${{ github.ref_name }}.apk
            dist/skyengine-${{ github.ref_name }}-arm64-v8a.apk
            dist/skyengine-${{ github.ref_name }}-armeabi-v7a.apk

      - name: Upload universal APK to file transfer
        id: upload_universal
        run: |
          DOWNLOAD_URL=$(node tool/upload-file.js "dist/skyengine-${GITHUB_REF_NAME}.apk" --direct)
          test -n "$DOWNLOAD_URL"
          echo "download_url=$DOWNLOAD_URL" >> "$GITHUB_OUTPUT"

      - name: Publish universal APK to update server
        env:
          MRP_SERVER: ${{ secrets.MRP_SERVER }}
          MRP_ACCESS_TOKEN: ${{ secrets.MRP_ACCESS_TOKEN }}
        run: |
          test -n "$MRP_SERVER"
          test -n "$MRP_ACCESS_TOKEN"
          node tool/publish_emulator_apk.js \
            --download-url "${{ steps.upload_universal.outputs.download_url }}" \
            --fetch-file \
            --architecture universal \
            --version "${{ steps.version.outputs.name }}" \
            --changelog-md CHANGELOG.MD

      - name: Upload arm64-v8a APK to file transfer
        id: upload_arm64
        run: |
          DOWNLOAD_URL=$(node tool/upload-file.js "dist/skyengine-${GITHUB_REF_NAME}-arm64-v8a.apk" --direct)
          test -n "$DOWNLOAD_URL"
          echo "download_url=$DOWNLOAD_URL" >> "$GITHUB_OUTPUT"

      - name: Publish arm64-v8a APK to update server
        env:
          MRP_SERVER: ${{ secrets.MRP_SERVER }}
          MRP_ACCESS_TOKEN: ${{ secrets.MRP_ACCESS_TOKEN }}
        run: |
          test -n "$MRP_SERVER"
          test -n "$MRP_ACCESS_TOKEN"
          node tool/publish_emulator_apk.js \
            --download-url "${{ steps.upload_arm64.outputs.download_url }}" \
            --fetch-file \
            --architecture arm64-v8a \
            --version "${{ steps.version.outputs.name }}" \
            --changelog-md CHANGELOG.MD

      - name: Upload armeabi-v7a APK to file transfer
        id: upload_armv7
        run: |
          DOWNLOAD_URL=$(node tool/upload-file.js "dist/skyengine-${GITHUB_REF_NAME}-armeabi-v7a.apk" --direct)
          test -n "$DOWNLOAD_URL"
          echo "download_url=$DOWNLOAD_URL" >> "$GITHUB_OUTPUT"

      - name: Publish armeabi-v7a APK to update server
        env:
          MRP_SERVER: ${{ secrets.MRP_SERVER }}
          MRP_ACCESS_TOKEN: ${{ secrets.MRP_ACCESS_TOKEN }}
        run: |
          test -n "$MRP_SERVER"
          test -n "$MRP_ACCESS_TOKEN"
          node tool/publish_emulator_apk.js \
            --download-url "${{ steps.upload_armv7.outputs.download_url }}" \
            --fetch-file \
            --architecture armeabi-v7a \
            --version "${{ steps.version.outputs.name }}" \
            --changelog-md CHANGELOG.MD
